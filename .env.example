# Create an OAuth application at https://osu.ppy.sh/home/account/edit#oauth
# (any callback URL works, it's unused for the client_credentials grant).
OSU_CLIENT_ID=
OSU_CLIENT_SECRET=

# Quaver's mapset search endpoint is public and needs no credentials.
